public enum EntityType
{
	Base,
	Player,
	Self,
	Monster,
}

public enum SceneType
{
    City = 1,  // 主城
    Battle = 2, // 战斗场景
}

public enum RecycleType
{
    ByUser,
    ByTime,
    ByChangeScene
}